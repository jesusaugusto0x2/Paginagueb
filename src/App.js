import React, { Component } from 'react';
import './App.css';

class App extends Component {
  render() {
    return (
      <React.Fragment>
        <h1>Ok so, I'm reworking this webpage again...</h1>    
        <p>I want it to look like a resumé, so don't judge me.</p>
      </React.Fragment>

    );
  }
}

export default App;
